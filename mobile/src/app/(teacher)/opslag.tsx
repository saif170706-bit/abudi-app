import { StudentPostsScreen } from '@/components/screens/student-posts-screen';

// Opslag is a shared, role-agnostic feed screen (audience filtering happens
// server-side in useAnnouncementsFeed) — reused as-is for teachers.
export default StudentPostsScreen;
